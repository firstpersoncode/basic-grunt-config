const js_one = () => {
  console.log('5');
};

const key = "value";

const initialObj = {
  key,
  test: "value-2",
  tests: "Value-3"
};

js_one();

$(".grunt-logo").click(function() {
  $(".col.right").toggleSlide()
});
